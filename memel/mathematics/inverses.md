# Inverse Functions

> one to one functions must pass HLT and VLT

## Finding Inverses

- x and y swap places
- $f^(-1)(x)$ found by solving for $y$
- $f(x)$ and $f^(-1)(x)$ are reflected over $y=x$
- $inverses \iff f(g(x)) = g(f(x))$