# Logarithms

## Logarithm Identities

- $log(xy) = log(x) + log(y)$
- $log(x / y) = log(x) - log(y)$
- $log(x^p) = p log(x)$
- $log_{b}(b^x) = b^{log_{b}(x)} = x$
- $log_{b}(a) = {log_{c}(a)} / {log_{c}(b)}$

