# common sense in linux

if the program structure is hierarchical then it can work in example as the following

```
>> input
<<   prints the entire fucking options list
<<     touchscreen ?:
<<     keyboard ?:
<<     touchpad ?:
<<     usb ?:
<<     powercable ?:
-- or
>> input help
>>   if you want to edit that program just type (input touchscreen on)
>>   and i will flip the hidden bit in the computing system that you cannot see or edit except via the basic
>>   ass input program that doesn't use command flags
```

```
if you think that sucks it does
because that is a (conf input touch off) command-esque input instead
```

---

```
nmcli or (alias nm=..)
```
