# Limit Comparison Test

if $\lim_{x \to \infty}{\frac{f(x)}{g(x)}} = L \mid 0 \lt L \lt \infty$ if **todo check $L$ is defined then both follow the same behavior**

$\implies \int_{a}^{\infty}{f(x)\delta x} \land \int_{a}^{\infty}{g(x)\delta x}$ both converge or diverge. *remember* greater than finite could be finite or infinite so direct comparison test fails