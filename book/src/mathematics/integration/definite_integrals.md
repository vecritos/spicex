# Definite Integrals

let $\lvert \lvert p \rvert \rvert$ be the largest subinterval of the partition $P_n$ of $[a,b]$ $\implies$

$\lim_{\lvert \lvert p \rvert \rvert \to 0}{\Sigma_{k=1}^{n}{f(c_k)\Delta x_k}} = \int_{a}^{b}{f(x)\delta x}$

if the partition of $[a,b]$ is made of equal subintervals ecah with width of $\Delta x$ then 

$\lim_{h \to \infty}{\Sigma_{k=1}^{n}{f(c_k)\Delta x}} = \int_{a}^{b}{f(x)\delta x}$

# Riemann Sums

${\Sigma_{k=1}^{n}{f(c_k)\Delta x_k}}$ is the $sum of areas of n rectangles (height of the kth rectangle * width of the kth rectangle$ this is known as a riemann sum