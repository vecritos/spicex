wraps an object and provices a different interface to it: facade
subclasses decide how to implement steps in an algorithm: template method
subclasses decide which concrete class to create: factory method
ensures one and only object is created: singleton
encapsulated interchangeable behaviors and uses delegation to decide which one to use: strategy
clients treat collections of objects and individual objects uniformly: composite
encapsulates state based behavriors and uses delegation to switch between behaviors: state
provides a way to traverse a collection of objects without exposing its implementation: iterator
simplifies the interface of a set of classes: facade
wraps an object to provide new behavior: decorator
allows a client to create families of objects without specifying their concrete class: abstract factory
allows objects to be notified when state changes: observer
wraps an object to control access to it: proxy
encapsulates a request as an object: command

head first design patterns


